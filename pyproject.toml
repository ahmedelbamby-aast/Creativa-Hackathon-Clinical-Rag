[project]
name = "creativa-diabetes"
version = "0.1.0"
description = "Bilingual diabetes RAG assistant"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi>=0.115.0",
    "google-genai>=1.0.0",
    # Keep the UI client and its URL parser on a tested pair.  Broad Gradio 4+
    # resolution previously left local environments with an incompatible
    # httpx build and caused collection-time InvalidURL failures.
    "gradio>=6.24.0,<7",
    "httpx>=0.28.1,<1",
    "langdetect>=1.0.9",
    "numpy>=1.26.0",
    "openai>=2.0.0",
    "pymupdf>=1.24.0",
    "pgvector>=0.4.0",
    "psycopg[binary]>=3.2.0",
    "pypdf>=4.0.0",
    "python-docx>=1.1.0",
    "python-dotenv>=1.0.0",
    "tqdm>=4.66.0",
]

[project.optional-dependencies]
local = [
    "sentence-transformers>=2.7.0",
    "torch>=2.7.0",
]

[project.scripts]
app = "backend.server:app"

[tool.vercel.fastapi.static]
cdn = false

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=7.0,<8",
    "pytest-instafail>=0.5,<1",
    "pytest-randomly>=4.0,<5",
    "pytest-timeout>=2.4,<3",
    "pytest-tldr>=0.2.6,<1",
    "pytest-xdist>=3.8,<4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = [
    "-n=4",
    "--dist=loadscope",
    "--instafail",
    "--timeout=30",
    "--timeout-method=thread",
    "--strict-config",
    "--strict-markers",
    "--cov=chunking",
    "--cov=example/services/security",
    "--cov=src",
    "--cov=app",
    "--cov-report=term-missing:skip-covered",
    "--cov-fail-under=75",
]
markers = [
    "integration: exercises multiple pipeline components together",
]

[tool.coverage.run]
branch = true
parallel = true
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 100
show_missing = true
skip_covered = true
exclude_also = ["if __name__ == .__main__.:"]
